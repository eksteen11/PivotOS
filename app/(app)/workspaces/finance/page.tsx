import { NotBuiltLane } from '@/components/workspace/NotBuiltLane'

export default function Page() {
  return (
    <NotBuiltLane
      name="Finance"
      note="Clark remains the finance system. Pivot only shows founder exceptions when that work is built."
    />
  )
}
