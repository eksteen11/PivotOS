import { NotBuiltLane } from '@/components/workspace/NotBuiltLane'

export default function Page() {
  return (
    <NotBuiltLane
      name="Product"
      note="Jira and GitHub own delivery. Pivot will show read-only status later, not a fake product workspace."
    />
  )
}
