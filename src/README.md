# Legacy Vite archive

This directory is the retired Pivot OS Vite prototype.

- It is excluded from the Next.js build and TypeScript project.
- It is not a production application.
- Do not add features here.
- The active product lives in `app/`, `components/`, and `lib/`.
